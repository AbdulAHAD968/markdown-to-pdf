export const metadata = {
    title: "World 1-2 (HTML)",
    description: "Convert HTML to PDF in World 1-2. Raw code to polished document.",
};

export default function HtmlLayout({ children }) {
    return <>{children}</>;
}
