export const metadata = {
    title: "World 1-1 (Markdown)",
    description: "Convert Markdown to PDF in World 1-1. The retro editor for your documentation needs.",
};

export default function MarkdownLayout({ children }) {
    return <>{children}</>;
}
